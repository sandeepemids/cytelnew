import { makeStyles } from "@material-ui/core/styles";

const marketAccessModelStyle = makeStyles((theme) => ({
  root: {
    marginTop: theme.spacing(3),
  },
  cardContent: {
    padding: "0 16px 27px 16px!important",
  },
}));

export default marketAccessModelStyle;
