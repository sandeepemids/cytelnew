import { makeStyles } from "@material-ui/core/styles";

const verticleBarStyle = makeStyles(theme => ({
  pipe: {
    margin: "0 9px",
    color: theme.palette.secondary.light
  }
}));

export default verticleBarStyle;
