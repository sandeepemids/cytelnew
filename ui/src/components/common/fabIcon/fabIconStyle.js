import { makeStyles } from "@material-ui/core/styles";

const fabIconStyle = makeStyles({
  root: {
    position: "absolute",
    right: "32px",
    bottom: "32px"
  }
});

export default fabIconStyle;
